# Pandoc Web

基于 pandoc/typst 的 Typst 编辑器，支持实时预览和多格式导出。

## 功能

- 创建 / 上传 / 编辑 `.typ` 文件，Typst 语法高亮
- 编辑时右侧实时预览（pandoc typst → HTML）
- 一键编译下载：PDF / EPUB / HTML / DOCX / Markdown / LaTeX

## 快速开始

```bash
docker compose up --build
```

访问 `http://localhost:5000`。
